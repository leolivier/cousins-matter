---
trigger: always_on
---

When running a test, first start the virtual environement with `source ./.venv/bin/activate` then to run the test, use `make test t=<test name>` which will ensure a proper environement.