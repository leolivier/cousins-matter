---
trigger: always_on
---

# Test running

* Before running a test, first start the virtual environement with `source ./.venv/bin/activate`
* To run the test, use `make test t=<test name>` which will ensure a proper environment.